# def 函数名():
# 子代码（函数体）--实现功能
# 函数里不固定的数据--定义成函数的参数--括号里
# 1、形参--函数定义的时候定义的
# 2、实参:调用函数传入参数
# 参教定义的类型:
# 1、必备参数:定义了就必须要传入的参数--不传会报错
# 2、默认参数（缺省参数）:可以定义的时候赋值一个默认值--调用的时候可以不传入；可
# 以传-替换掉默认值。
# 注意：默认参数必须跟在必备参数后面！！
# 3、不定长参数：等前面的必备参数和默认参数都接受完了，剩下的参数都给不定长参数接
# 受
# *args 接受不确定数量，个数的参数 -- 可以不传，可以传入（1 个、多个）
# 元组接收 传参方式：按照位置来传
# **kwargs:字典接收 传参方式：关键字传参